% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom-adapter-block.R
\name{new_broom_block}
\alias{new_broom_block}
\title{Broom Adapter Block}
\usage{
new_broom_block(
  output = "tidy",
  conf_int = TRUE,
  conf_level = 0.95,
  qq = FALSE,
  ...
)
}
\arguments{
\item{output}{One of \code{"tidy"}, \code{"glance"}, \code{"augment"}.}

\item{conf_int, conf_level}{Add CIs to \code{tidy} (default \code{TRUE}, \code{0.95}).}

\item{qq}{Logical, add QQ columns to \code{augment} (default \code{FALSE}).}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A transform block of class \code{broom_block}.
}
\description{
Transform block: takes a fitted model (upstream) and emits a tidy
data frame via standard \code{broom} verbs. One block, three outputs
(\code{tidy}/\code{glance}/\code{augment}) selectable in the UI. The tidy frame is
the renderer boundary -- feed it to the generic drilldown chart/table
(coef plot, glance table, residual/QQ scatter).
}
\details{
The verb picker is the whole face of the block. Everything else is
verb-specific and lives in the gear band, which shows only the options
the selected verb actually has: \code{conf.int} / \code{conf.level} for \code{tidy}
(broom's own arguments) and QQ columns for \code{augment} (a blockr.stats
convenience, so a QQ plot is a plain scatter). \code{glance()} has no
options, so on \code{glance} there is no gear.

The generated expression is plain \code{broom::tidy()} / \code{glance()} /
\code{augment()}; no blockr.stats function appears in the generated code.
}
\examples{
if (interactive()) {
  library(blockr.core)
  serve(
    new_broom_block(output = "tidy"),
    data = list(data = lm(mpg ~ wt + hp, mtcars))
  )
}
}
