% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-block-explorer.R
\name{new_model_explorer_block}
\alias{new_model_explorer_block}
\title{Model Explorer Block (experimental)}
\usage{
new_model_explorer_block(
  model_type = "lm",
  formula = list(response = NULL, intercept = TRUE, terms = list(), bars = list(), offset
    = NULL, weights = NULL),
  ...
)
}
\arguments{
\item{model_type}{One of \code{"lm"}, \code{"logistic"}, \code{"poisson"}, \code{"gamma"}.}

\item{formula}{Structured formula-input state: a list with \code{response},
\code{intercept}, \code{terms}, \code{bars}, \code{offset}, \code{weights} (see \code{parse_formula()}).}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A transform block of class \code{model_explorer_block}.
}
\description{
EXPERIMENTAL variant of \code{\link[=new_model_block]{new_model_block()}}. Identical behaviour and R-side
formula handling, but the UI uses the \strong{formula-explorer widget}
(\code{\link[=formula_explorer_ui]{formula_explorer_ui()}}) instead of the formula-input widget. The explorer
forks \code{Blockr.Select} (as \code{BlockrX.multi}) to support clicking predictor
chips to select them and right-clicking the selection to cross them into an
interaction term (or remove them).
}
\details{
All R-side helpers are shared with the model block
(\code{parse_formula()} / \code{make_model_formula()} / \code{build_model_call()} /
\code{build_formula_columns()} / \code{formula_col_types()} /
\code{normalize_formula_for_js()}), so interactions/transforms/splines refit
automatically with no additional R code.
}
