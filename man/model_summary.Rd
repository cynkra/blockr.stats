% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-summary.R
\name{model_summary}
\alias{model_summary}
\title{Summarise a fitted model for display}
\usage{
model_summary(
  x,
  uncertainty = "ci95",
  significance = "chips",
  scale = "auto",
  effect_column = TRUE,
  facts = TRUE,
  intercept = TRUE
)
}
\arguments{
\item{x}{A fitted model object, or a tidy coefficient data frame.}

\item{uncertainty}{What the interval column and whisker show: \code{"ci95"}
(default), \code{"ci90"}, \code{"ci99"}, \code{"se"} (estimate +/- one standard error,
about 68\%, \emph{not} a confidence interval) or \code{"none"}.}

\item{significance}{How significance is displayed: \code{"chips"} (default;
0.1\% / 1\% / 5\% coloured, 10\% grey), \code{"p"} (a p-value column), \code{"stars"}
(\verb{***} / \verb{**} / \code{*} / \code{.}) or \code{"none"}.}

\item{scale}{\code{"auto"} (default) exponentiates coefficients to odds / rate /
hazard ratios when the model uses a log or logit link and leaves them
alone otherwise; \code{"ratio"} and \code{"raw"} force it either way. On the ratio
scale the reference line moves from 0 to 1 and the axis becomes
logarithmic.}

\item{effect_column}{Draw the inline forest column (default \code{TRUE}).}

\item{facts}{Show the one-line model facts stripe (default \code{TRUE}).}

\item{intercept}{Keep the intercept row (default \code{TRUE}). It is always kept
out of the forest's scale, so it never squashes the other terms.}
}
\value{
A data frame of coefficients, of class \code{model_summary}, with the
glance frame, model facts and display options attached as attributes.
}
\description{
Takes a \strong{fitted model object} and returns its coefficient table
(\code{broom::tidy()}) with the fit statistics (\code{broom::glance()}) and the
display options carried along as attributes. This is the value behind the
model summary block: the returned object is an ordinary data frame of
coefficients downstream, and \code{\link[=model_summary_card]{model_summary_card()}} renders it as the card.
}
\details{
Any model with a \code{broom::tidy()} method works, whether or not blockr.stats
has a block for it -- an \code{lm} from the model block, a \code{coxph} from the
survival block, or something fitted in a function block. No broom adapter
block in between: the tidying happens here.

A tidy coefficient \strong{frame} (\code{term} + \code{estimate}, e.g. straight from the
broom adapter block) is also accepted and used as-is; only the model facts
are then unavailable.
}
\examples{
model_summary(lm(mpg ~ wt + hp, mtcars))
}
