% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlate-block.R
\name{new_correlate_block}
\alias{new_correlate_block}
\title{Correlate Block}
\usage{
new_correlate_block(vars = character(), method = "pearson", ...)
}
\arguments{
\item{vars}{Numeric column names to correlate.}

\item{method}{One of \code{"pearson"}, \code{"spearman"}, \code{"kendall"}.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A \code{correlate_block} transform block.
}
\description{
Transform block wrapping \code{\link[=correlate_numeric]{correlate_numeric()}}. Emits a tidy
correlation matrix (a \code{var} row-label column plus one numeric column
per selected variable) for the generic renderers; feed it to the
table block with \code{rowname = "var"} and a diverging \code{cell_color} for a
heatmap.
}
\examples{
if (interactive()) {
  library(blockr.core)
  serve(new_correlate_block(), data = list(data = mtcars))
}
}
