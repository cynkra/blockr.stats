% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlate-block.R
\name{correlate_numeric}
\alias{correlate_numeric}
\title{Pairwise correlation matrix (base R)}
\usage{
correlate_numeric(data, vars = NULL, method = "pearson", digits = 2L)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Numeric column names to correlate (default: all numeric).}

\item{method}{One of \code{"pearson"}, \code{"spearman"}, \code{"kendall"}.}

\item{digits}{Decimal places to round the correlations to.}
}
\value{
A tidy correlation-matrix data frame.
}
\description{
Correlation matrix of the selected numeric columns, returned as a
tidy data frame: a leading \code{var} character column (the row variable)
plus one numeric column per selected variable. Values are rounded
correlations. Uses \code{use = "pairwise.complete.obs"}. Renders directly
as a heatmap via the table block (\code{rowname = "var"} + a diverging
\code{cell_color}).
}
\examples{
correlate_numeric(mtcars, vars = c("mpg", "hp", "wt"))
}
