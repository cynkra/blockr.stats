% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-summary-block.R
\name{new_model_summary_block}
\alias{new_model_summary_block}
\title{Model Summary Block}
\usage{
new_model_summary_block(
  uncertainty = "ci95",
  significance = "chips",
  scale = "auto",
  effect_column = TRUE,
  facts = TRUE,
  intercept = TRUE,
  ...
)
}
\arguments{
\item{uncertainty}{What the interval column and whisker show: \code{"ci95"}
(default), \code{"ci90"}, \code{"ci99"}, \code{"se"} or \code{"none"}.}

\item{significance}{\code{"chips"} (default; 0.1\% / 1\% / 5\% coloured, 10\%
grey), \code{"p"} (a p-value column), \code{"stars"} or \code{"none"}.}

\item{scale}{\code{"auto"} (default), \code{"raw"} or \code{"ratio"}.}

\item{effect_column, facts, intercept}{Show the forest column / the model
facts line / the intercept row (all \code{TRUE} by default).}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A transform block of class \code{model_summary_block}.
}
\description{
Takes a \strong{fitted model} and renders it as a dashboard-quality card: a
one-line model facts stripe (kind, n, the fit measures that suit the model
class) over a coefficient table whose first column is an inline forest --
estimate dot, uncertainty whisker, reference line and a shared axis.
}
\details{
The block feeds on the model object directly. It calls \code{broom::tidy()} and
\code{broom::glance()} itself, so no adapter block is needed in between and any
model with a broom method works, whether or not blockr.stats has a block
that fits it: an \code{lm} from the model block, a \code{coxph} from the survival
block, or anything fitted in a function block. A tidy coefficient frame
(\code{term} + \code{estimate}) is accepted too, in which case the model facts are
simply unavailable.

Its \strong{value} is the coefficient data frame, so a table, chart or report
exhibit downstream sees an ordinary tidy frame; the card is that frame
drawn. Every option lives in the gear -- the card itself is the block's
face.
}
\examples{
if (interactive()) {
  library(blockr.core)
  serve(
    new_model_summary_block(),
    data = list(data = lm(mpg ~ wt + hp, mtcars))
  )
}
}
