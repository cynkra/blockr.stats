% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom-adapter-block.R
\name{broom_apply}
\alias{broom_apply}
\title{Apply a broom verb to a fitted model}
\usage{
broom_apply(
  model,
  output = "tidy",
  conf_int = TRUE,
  conf_level = 0.95,
  qq = FALSE
)
}
\arguments{
\item{model}{A fitted model object.}

\item{output}{One of \code{"tidy"}, \code{"glance"}, \code{"augment"}.}

\item{conf_int}{Logical, add CIs to \code{tidy} (default TRUE).}

\item{conf_level}{Confidence level (default 0.95).}

\item{qq}{Logical, add QQ columns to \code{augment} (default FALSE).}
}
\value{
A tidy \code{data.frame}.
}
\description{
The single generic adapter that turns any fitted model into a tidy
data frame for the generic renderers. \code{tidy} -> coefficient table
(coef plot via drilldown lo/hi); \code{glance} -> one-row fit summary;
\code{augment} -> per-observation frame for diagnostic scatters. With
\code{qq = TRUE} the augmented frame also gets \code{.qq_theoretical} /
\code{.qq_sample} so a QQ plot is a plain scatter.
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
broom_apply(fit, output = "tidy")
broom_apply(fit, output = "glance")
}
